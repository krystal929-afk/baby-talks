# New Supabase migration

Use `001_baby_firefly_secure_schema.sql` on the NEW empty Supabase project.

Do **not** replay the legacy migrations from `../migrations/` on the new database. They are kept only as Lovable-era history and contain earlier broad-access policies.
